<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="shortcut icon" type="image/x-icon" href="../../img/favicon.svg">
    <title>Login</title>
    <link rel="stylesheet" href="../css/bootstrap.css">
    <link rel="stylesheet" href="../css/general.css">
    <link rel="stylesheet" href="../css/animate.css">
    <script src="../js/jq-bs-bundle.js"></script> 
</head>
<body class="d-flex flex-column h-100 bg-light">
    <div id="fondoImagen"></div>
    <div id="fondoColor"></div>
    <style>
        #fondoImagen {
            background: url('/img/IMG_001.jpg'); 
            background-size: cover;
            filter: blur(7px);
            position: absolute;
            top: 0;
            left: 0;
            z-index: 1;
            width: 100%;
            height: 100%;
        }
        #fondoColor {
            background: rgba(0, 79, 152, 0.5);
            position: absolute;
            top: 0;
            left: 0;
            z-index: 2;
            width: 100%;
            height: 100%;
        }
    </style>
    <div class="container my-auto" style="z-index: 3;">
        <div class="w-100 m-auto bg-white shadow rounded-lg p-3 animate__animated animate__zoomIn" style="max-width: 500px;">
            <img class="mx-auto my-3 d-block" src="../img/logo-vive-promocion.png" alt="" width="40%">
            <div class="w-100 m-auto" style="max-width: 450px;">
                <%if(typeof err != "undefined"){%>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <strong>Login Failed!</strong> <%= err%>
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                <%}%>
                <%if(typeof fb != "undefined"){%>
                    <%if(fb.text == "GOOD"){%>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <strong>Ha cambiado su clave!</strong>  <%= fb.feed%>
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    <%}%>
                <%}%>
                <form action="/client/login" method="post">
                    <label for="inputEmail">Cedula</label>
                    <input type="text" id="cedula" name="cedula" class="form-control mb-3" placeholder="Cedula..." required="" autofocus="">
                    
                    <label for="inputPassword">Clave</label>
                    <input type="password" id="clave" name="clave" class="form-control" placeholder="Clave..." required="">
                    <a href="/cambiarClave">Olvide mi contraseña</a>
                    <button class="btn btn-lg btn-secondary btn-block mt-4" type="submit" id="inciarSesion">Entrar</button>
                </form>
                <a href="/registrarse" class="btn btn-primary my-4 mx-auto d-block">Registrarse</a>
            </div>
        </div>
    </div>
    <footer class="footer justify-content-center" style="z-index: 3;">
        <p class=" text-center text-white">© 2020-2021 Universidad Nacional de Costa Rica</p>
   </footer>
    <script src="../js/login.js"></script>
</body>
</html>